# Liangxu Wei — Academic Homepage

A concise academic personal homepage designed for GitHub Pages.

## Update the content

The main profile content is in `app/page.tsx`; typography and layout are in
`app/globals.css`. Push changes to the `main` branch and the included GitHub
Pages workflow will publish the updated site automatically.
